import 'package:flutter/material.dart';

class AppButtons {
  AppButtons._();
  // ADD ARGUMENTS
  static Widget circularButton({required String title}) {
    return const Placeholder();
  }

  static Widget circularButtonWithIcon({
    required String title,
    required IconData iconData,
  }) {
    return const Placeholder();
  }
}
