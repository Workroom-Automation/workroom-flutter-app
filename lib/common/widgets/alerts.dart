//  ADD HERE ALL THE ALERT TYPE APP CAN USE
enum Alert { popUp, toast, snackbar }

enum AlertType { error, success, info, failure }

class AppAlerts {
  bool showAlerts = true;
// SHOW TOAST

// SHOW SNACKBAR

// SHOW POPUP
  void showPopUp() {
    if (!showAlerts) return;
    //
  }
}
